{
  "name": "branches-neon-lab",
  "private": true,
  "version": "0.1.0",
  "description": "Standalone educational BRANCHES Neon Lab frontend surface",
  "scripts": {
    "start": "vite --host 127.0.0.1 --port 5177",
    "dev": "vite --host 127.0.0.1 --port 5177",
    "build": "vite build --outDir lab-dist --emptyOutDir",
    "preview": "vite preview --host 127.0.0.1 --port 4177"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
